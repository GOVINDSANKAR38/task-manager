{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "A simple task manager using JSON Server as the backend",
  "main": "app.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "task-manager",
    "json-server",
    "frontend-backend"
  ],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.2"
  }
}
